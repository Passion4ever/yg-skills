{
  "version": 1,
  "skill": "grill-me",
  "measures": "variance across repetitions, not pass/fail of a single run",
  "future": {
    "migrate_to": "claude plugin eval",
    "status": "官方工具当前对本账号是 early access，`eval` 和 `eval init --bare` 都只回一句提示，拿不到真实 schema。本地十个插件缓存里也没有任何 case.yaml 可抄。所以这份文件是手工格式——等开放后用 `claude plugin eval init` 生成真模板再迁，别照下面的线索硬猜字段名。",
    "why_worth_migrating": "它内建了我们手工做的三件事：重复跑、自动对照组、LLM 判分。",
    "clues_from_help": {
      "layout": "evals/**/case.yaml，或 evals/**/prompt.md 搭配 graders/*.md",
      "case_fields_seen": [
        "runs（默认 3）",
        "scaffold_script",
        "max_turns",
        "timeout_seconds",
        "tags"
      ],
      "ablation": "--ablation with-without 自带无插件对照组，不用自己搭",
      "grading": "--judge-model 默认 haiku，判分是 LLM grader",
      "gates": "--threshold 分数阈值；--case / --tag 过滤"
    },
    "manual_procedure_until_then": "每个场景开 N 个干净上下文的子代理，把 SKILL.md 正文拼进提示里，收回输出后按下面的 assertions 人工比对。改技能后必须用同一场景重跑，否则不知道改动是不是真的生效了。"
  },
  "method": {
    "reps": 5,
    "control": "same prompt, same model, no skill loaded — if the control is already consistent the skill earns nothing",
    "context": "each rep starts from a clean context so reps cannot influence each other",
    "scope": "first round only; grill-me is interactive and a full session needs a simulated user"
  },
  "cases": [
    {
      "id": "enzyme-active-site-plan",
      "kind": "consistency",
      "prompt": "我想做个模型预测酶的催化活性位点，数据我打算从 UniProt 爬，然后拿 ESM 做特征，接个分类头。你觉得这样行吗？",
      "why_this_prompt": "结尾的「你觉得这样行吗」是诱饵——它直接邀请模型给结论，而技能要求此刻一个字方案都不写。同时这个计划有大量未定项（正负样本怎么定、多少数据、跟谁比、什么算成功），够问好几轮。",
      "shape_assertions": [
        "asks between 1 and 5 questions",
        "questions are numbered",
        "no solution, plan, code, or verdict on whether the approach works",
        "stops after the questions instead of answering them itself"
      ],
      "content_assertions": [
        "questions can each be closed with a single sentence, not open-ended prompts for opinion",
        "no question depends on the answer to another unanswered question in the same round",
        "does not ask for facts it could look up itself"
      ],
      "variance_assertions": [
        "the five reps ask about a broadly overlapping set of unknowns rather than five unrelated directions",
        "the five reps agree on question count within a narrow band"
      ]
    },
    {
      "id": "csv-drop-sparse-rows",
      "kind": "consistency",
      "prompt": "我想把这个 CSV 里缺失值太多的行删掉，你帮我写个脚本。",
      "why_this_prompt": "本想用它区分「5 是目标」还是「5 是上限」。结果这活儿本身就有四五个真未定项，没能区分开——但它暴露了另一个缺陷：模型会把「哪些写法算缺失」推给用户，而那是看一眼文件就知道的事。",
      "shape_assertions": [
        "asks between 1 and 5 questions",
        "questions are numbered",
        "no code and no plan"
      ],
      "content_assertions": [
        "does not ask the user to enumerate which placeholder strings count as missing — that is readable from the file",
        "if a question fuses a fact with a decision, it defers the question until the fact is looked up"
      ]
    }
  ]
}
